var services = angular.module('services', []);

services.factory("ticketForAllService", function ($http) {
    var service = {};

    //aqui van les operacions del servei com a atributs de l'objecte service

    return service;
});

